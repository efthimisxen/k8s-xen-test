apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app-deployment
spec:
  template:
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.3
        ports:
        - containerPort: 80